//Used in the profile pages to show the basic info about the user themselves
import React from "react";
import { Link } from "react-router-dom";

const UserInfo = () => {
    
};

export default UserInfo;