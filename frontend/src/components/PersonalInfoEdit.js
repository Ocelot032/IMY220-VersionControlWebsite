import React from "react";

const PersonalInfoEdit = () => {
//only a button gets displayed "Edit Profile", but 
// when it is clicked, the user gets all the prompts 
// to change their details. They also have the option 
// to exit out of it.
// OR
// User gets a simple popup for the info change
// if statement in main page - if button is clicked, this pops up?
    return (
        <div>
            <button>Edit Profile</button>
        </div>
    );
};

export default PersonalInfoEdit;